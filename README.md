# 💬 Project_YV — Консольный мессенджер на Go

Project_YV — это консольный мессенджер, написанный на языке Go с использованием PostgreSQL в качестве базы данных. Он позволяет пользователям регистрироваться, входить в систему, менять свои данные (имя, username, пароль) и обмениваться сообщениями с другими пользователями по ID. Все данные хранятся в базе, пароли хэшируются с помощью bcrypt, а конфигурация проекта задаётся через .env файл.

Чтобы запустить проект, сначала нужно установить зависимости командой go mod tidy. Затем создать файл .env в корне проекта и прописать в нём параметры подключения к базе данных: DB_HOST, DB_PORT, DB_USER, DB_PASSWORD и DB_NAME. Пример содержимого .env файла:

DB_HOST=localhost  
DB_PORT=5432  
DB_USER=postgres  
DB_PASSWORD=123123  
DB_NAME=postgres

После этого необходимо создать таблицы в базе данных. Для этого можно использовать следующий SQL:

CREATE TABLE users (  
    id SERIAL PRIMARY KEY,  
    username TEXT UNIQUE NOT NULL,  
    password TEXT NOT NULL,  
    name TEXT NOT NULL  
);

CREATE TABLE messages (  
    id SERIAL PRIMARY KEY,  
    sender_id INTEGER REFERENCES users(id),  
    receiver_id INTEGER REFERENCES users(id),  
    content TEXT NOT NULL,  
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP  
);

Когда база готова, можно собрать проект командой go build -o messenger и запустить ./messenger. После запуска откроется главное меню, где можно выбрать вход, регистрацию или выход. При регистрации система проверяет, что username уникален, и не позволяет вводить пустые значения. После входа пользователь попадает в меню, где может изменить свои данные или начать чат с другим пользователем, указав его ID. Сообщения сохраняются в таблице messages, и при открытии чата отображается история переписки.

Проект использует следующие библиотеки: github.com/lib/pq для работы с PostgreSQL, github.com/joho/godotenv для загрузки переменных окружения из .env, и golang.org/x/crypto/bcrypt для безопасного хранения паролей. Все пароли хранятся в виде хэшей, а .env файл добавлен в .gitignore, чтобы не попасть в репозиторий.

Автор проекта — Евгений (GitHub: @swoggoi), Иван -(GitHub: @swogga). Сделано с болью, матами и Go ❤️
